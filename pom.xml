<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>sh.app</groupId>
  <artifactId>ticket-printer</artifactId>
  <packaging>jar</packaging>
  <version>1.0.0.0</version>
  <name>Ticket printer</name>


  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>


  <profiles>
    <profile>
      <id>dev</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <build>
        <resources>
          <resource>
            <directory>src/main/resources/common</directory>
          </resource>
          <resource>
            <directory>src/main/resources/dev</directory>
          </resource>
        </resources>
      </build>
    </profile>

    <profile>
      <id>prod</id>
      <build>
        <resources>
          <resource>
            <directory>src/main/resources/common</directory>
          </resource>
        </resources>
        <plugins>
          <plugin>
            <artifactId>maven-antrun-plugin</artifactId>
            <executions>
              <execution>
                <phase>install</phase>
                <goals>
                  <goal>run</goal>
                </goals>
                <configuration>
                  <tasks>
                    <copy todir="${project.build.directory}/prod">
                      <fileset dir="${basedir}/src/main/resources/prod_rest" />
                    </copy>
                    <copy file="${project.build.directory}/ticket-printer-${version}.jar" tofile="${project.build.directory}/prod/ticket-printer__V${version}.jar" />
                    <copy file="${settings.localRepository}/commons-codec/commons-codec/1.4/commons-codec-1.4.jar" todir="${project.build.directory}/prod" />
                  </tasks>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>com.google.code.maven-replacer-plugin</groupId>
            <artifactId>maven-replacer-plugin</artifactId>
            <version>1.3.1</version>
            <executions>
              <execution>
                <phase>install</phase>
                <goals>
                  <goal>replace</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <file>target/prod/sample.html</file>
              <file>target/prod/ticket-printer.jnlp</file>
              <replacements>
                <replacement>
                  <token>PRINTER_APPLET_VERSION</token>
                  <value>${version}</value>
                </replacement>
              </replacements>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>show</id>
      <build>
        <resources>
          <resource>
            <directory>src/main/resources/common</directory>
          </resource>
        </resources>
        <plugins>
          <plugin>
            <artifactId>maven-antrun-plugin</artifactId>
            <executions>
              <execution>
                <phase>install</phase>
                <goals>
                  <goal>run</goal>
                </goals>
                <configuration>
                  <tasks>
                    <copy file="${basedir}/src/main/resources/prod_rest/sample.html" todir="${basedir}/../webpage/src/sh/app/ticket_printer_webpage" overwrite="true" />
                    <copy file="${basedir}/src/main/resources/prod_rest/deployJava.js" todir="${basedir}/../webpage/WebContent" overwrite="true" />
                    <copy file="${basedir}/src/main/resources/prod_rest/ticket-printer.jnlp" todir="${basedir}/../webpage/WebContent" overwrite="true" />
                    <copy file="${project.build.directory}/ticket-printer-${version}.jar" tofile="${basedir}/../webpage/WebContent/ticket-printer__V${version}.jar" overwrite="true" />
                    <copy file="${settings.localRepository}/commons-codec/commons-codec/1.4/commons-codec-1.4.jar" todir="${basedir}/../webpage/WebContent" overwrite="true" />
                  </tasks>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>com.google.code.maven-replacer-plugin</groupId>
            <artifactId>maven-replacer-plugin</artifactId>
            <version>1.3.1</version>
            <executions>
              <execution>
                <phase>install</phase>
                <goals>
                  <goal>replace</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <basedir>../webpage</basedir>
              <filesToInclude>
                src/sh/app/ticket_printer_webpage/sample.html,
                WebContent/ticket-printer.jnlp
              </filesToInclude>
              <replacements>
                <replacement>
                  <token>PRINTER_APPLET_VERSION</token>
                  <value>${version}</value>
                </replacement>
              </replacements>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>


  <dependencies>
    <dependency>
      <groupId>commons-codec</groupId>
      <artifactId>commons-codec</artifactId>
      <version>1.4</version>
    </dependency>
  </dependencies>


  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>1.6</source>
          <target>1.6</target>
          <encoding>UTF-8</encoding>
        </configuration>
      </plugin>

      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <phase>process-resources</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <tasks>
                <echo file="${basedir}/src/main/resources/common/META-INF/VERSION" encoding="UTF-8" message="${version}" />
              </tasks>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>